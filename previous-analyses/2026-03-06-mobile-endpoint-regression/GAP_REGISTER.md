# Gap Register

| id | domain | surface | severity | user_impact | evidence | current_behavior | expected_behavior | fix_strategy | test_strategy | owner_suggestion |
|---|---|---|---|---|---|---|---|---|---|---|
| G-001 | APIv4 mobile bootstrap | `GET /api/v4/system/ping`, `GET /api/v4/system/version`, related v4 routers replaced by stubs | P0 | Mobile app cannot complete initial server validation and appears broken after latest build | Upstream: `../mattermost/api/v4/source/system.yaml:24`, `../mattermost/server/channels/api4/system.go:42,145-153`; Mobile client usage: `../mattermost-mobile/app/client/rest/general.ts:32-41`; Current Rustchat stub: `backend/src/api/v4/system.rs:1-6`; Last commit added stubs: `git show --name-status 259f42d`; previous commit archived full implementations: `git show --name-status 2f5076d` | Active `system::router()` is empty and required routes are not registered | Keep system bootstrap endpoints and related compatibility routers implemented in active backend source | Restore archived route implementations from `archive/20260306-163915-remaining-dirty/backend/src/api/v4/*.rs` back to active `backend/src/api/v4/` for affected modules | Run `cargo check`, `cargo test --test api_v4_plugins_dialogs -- --nocapture`, and `BASE=<server-endpoint> ./scripts/mm_mobile_smoke.sh` | Backend API compatibility owner |
